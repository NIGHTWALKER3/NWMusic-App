name: music_app
description: Ad-free royalty free music streaming app
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^1.3.6        # updated to latest stable
  just_audio: ^0.9.36
  cupertino_icons: ^1.0.6
  provider: ^6.0.5    # optional, if you use ChangeNotifier in your provider

flutter:
  uses-material-design: true
